The RTBuilder tool will output data files to this directory when run.

Make sure you run the builder before attempting to run the OfflineRT sample as
it expects the generated data files to be present here.